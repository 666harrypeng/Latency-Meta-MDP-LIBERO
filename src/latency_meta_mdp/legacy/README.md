# Historical implementations

These modules retain earlier Gaussian, Flow, information-state and return-mixture
experiments. Current entrypoints do not import them. Matching configurations are
under `configs/legacy`.

Historical tools can refer to retired datasets. Use the code and data identities
recorded with an earlier run when reproducing it.
