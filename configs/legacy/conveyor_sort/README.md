# Historical conveyor configurations

- `overview-view.yaml`: the unselected full-table camera preview.
- `panda_osc_pose_delta_conveyor_v1.yaml`: the earlier decoupled OSC profile.

Current task configuration is `configs/tasks/conveyor_sort/surface.yaml`; it
selects the coupled `panda_osc_pose_delta_conveyor_v2` controller. These historical
profiles are available for explicit replay and are not current training defaults.
