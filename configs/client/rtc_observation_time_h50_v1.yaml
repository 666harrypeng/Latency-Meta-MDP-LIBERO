schema_version: 1
protocol_id: rtc_observation_time_h50_v1
temporal_contract: ../temporal/h50_e25_d20_k6_v1.yaml
delay_history_capacity: 32
# Conservative configured prior, not an observed latency. Real runs may replace
# this with explicitly recorded, independent warm-up calibration before the run.
initial_delay_ticks: [20]

